import React from 'react'
import ReactDOM from 'react-dom/client'
import LoaderPage from './client/src/components/LoaderPage'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LoaderPage />
  </React.StrictMode>,
)