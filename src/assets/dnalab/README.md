Background and specimen images are referenced by absolute URL
(e.g. `/assets/dnalab/research-door.webp`) and therefore live in
`/public/assets/dnalab/`, not here — Vite serves `public/` at the
site root. This folder is kept for future phases that may need
locally-imported (bundled) image assets instead of public ones.
