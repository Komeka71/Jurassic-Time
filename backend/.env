# Rename this file to .env and fill in real values. Never commit the real .env file.

PORT=5000
NODE_ENV=development

# Local MongoDB or a MongoDB Atlas connection string
MONGO_URI=mongodb+srv://Trex:jurassic123@jurassictime.zzn1yqi.mongodb.net/jurassic-time?retryWrites=true&w=majority

# Long random string used to sign JWTs. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=7088d72cf0f4397b0b2a56b8b11c858435977a523f75662de5f148574fde06f3bd24f6beb77d82f23aaa38e8895a8a76

# How long the login session lasts
JWT_EXPIRES_IN=7d

# URL of the frontend, used for CORS
CLIENT_URL=http://localhost:5173

# --- Google Sign-In ---
# From https://console.cloud.google.com/apis/credentials -> Create OAuth client ID
# -> Web application. Add http://localhost:5173 as an Authorized JavaScript origin.
# Same value also goes in frontend/.env as VITE_GOOGLE_CLIENT_ID.
GOOGLE_CLIENT_ID=269071457782-hd0m3uv995hkrikbh6clbq82fstpqpku.apps.googleusercontent.com

# --- Email OTP (using Gmail SMTP) ---
# EMAIL_USER: a Gmail address you control.
# EMAIL_PASS: a 16-character Gmail "App Password" (NOT your normal Gmail password).
# Generate one at https://myaccount.google.com/apppasswords (requires 2-Step
# Verification to be turned on for the Google account first).
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=divein.dino.world@gmail.com
EMAIL_PASS=krirzdeiywqcqlbz
EMAIL_FROM=divein.dino.world@gmail.com
